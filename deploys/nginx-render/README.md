# nginx

Image: `nginx:latest`

## Render

This package includes `render.yaml` for Render Blueprint deploys.

## Notes

Service type: `web service`
Port detected: `8080`
No persistent disk generated.
Original healthcheck: `wget -qO- http://127.0.0.1 || exit 1`
